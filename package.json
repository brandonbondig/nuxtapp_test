{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "preprepare": "npm install nuxt",
    "postinstall": "nuxt prepare",
    "deploy": "npm install && npm run preprepare && run build && node ./output/server/index.mjs"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "nuxt": "^3.7.1"
  }
}
